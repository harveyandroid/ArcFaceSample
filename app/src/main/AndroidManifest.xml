<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.harvey.arcfacedamo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.harvey.arcfacedamo.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.harvey.arcfacedamo.ui.FaceScanActivity"></activity>
        <activity android:name="com.harvey.arcfacedamo.ui.FaceRegisterActivity"></activity>

        <meta-data
            android:name="com.harvey.image.CustomCachingGlideModule"
            android:value="GlideModule" />
    </application>

</manifest>